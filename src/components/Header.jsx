import Counter from './header/Counter';
import Logo from './header/Logo';

const Header = () => {
  return (
    <header>
      <Logo />

      <Counter />
    </header>
  );
}

export default Header;
